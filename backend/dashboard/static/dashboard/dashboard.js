(function () { const node = document.getElementById("registration-data"); const canvas = document.getElementById("registration-chart"); if (!node || !canvas) return; const data = JSON.parse(node.textContent); const ctx = canvas.getContext("2d"); const max = Math.max(1, ...data.map((x) => x.count)); const width = canvas.clientWidth || 600; canvas.width = width; data.forEach((point, i) => { const x = (i / Math.max(1, data.length - 1)) * width; const y = canvas.height - (point.count / max) * (canvas.height - 20); ctx.fillStyle = "#2563eb"; ctx.fillRect(x - 2, y, 4, canvas.height - y); }); })();
